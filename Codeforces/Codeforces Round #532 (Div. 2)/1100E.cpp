#include<bits/stdc++.h>
using namespace std;
const int MAXN = 1e5 + 5;
int n, m;
struct Edge
{
    int from, to, cost;
    Edge() {}
    Edge(int from, int to, int cost) : from(from), to(to), cost(cost) {}
}
vector<Edge> edges;
vecotr<int> G[MAXN];
int main()
{
    scanf("%d%d", &n, &m);
    while (m--)
    {

    }
    return 0;
}